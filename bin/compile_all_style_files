#!/usr/bin/env bash
# -*- bash -*-
#
#!/usr/bin/env node
# -*- js   -*-
#
files="$@"
set -u -e -o pipefail



if [[ -z "$files" ]]
then
  files=$(find ./Public -iregex '.*\.styl')
fi

for i in $files
do
  stylus $i
done
