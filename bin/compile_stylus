#!/usr/bin/env bash
# -*- bash -*-
#
#!/usr/bin/env node
# -*- js   -*-
#
files="$1"
set -u -e -o pipefail



if [[ -z "$files" ]]
then
  files=$(find ./Public -iregex '.*\.styl')
else
  if [[ "$files" =~ "Public/css" ]]
  then
  else
    stylus $file
  fi
fi

