#!/usr/bin/env bash
# -*- bash -*-
#

mods=(
Customer
Screen_Name
Permission
Follow
Chit_Chat
Comment
Notify
Bot_Code
Bot_Use
)

# === Create a reverse of the list.
rev_mods=()

for (( idx=${#mods[@]}-1 ; idx >= 0 ; idx-- ))
do
  rev_mods+=( "${mods[idx]}" )
done
