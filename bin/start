#!/usr/bin/env bash
# -*- bash -*-
#
set -o pipefail
set -e
PORT="$PORT"
echo "$PORT"
set -u


if [[ -z "$PORT" ]]
then
  PORT="5000";
fi

if [[ -n "$@" ]]
then
  PORT="$@"
fi

set -u

NODE_ENV=production PORT=$PORT exec node Server/App/app.js $PORT
